/**
 * Create an interface with OpenAI, e.g. ChatGPT. You'll need
 * to provide credentials and so on, but
 * @param {string} apiKey - The API key needed to access the OpenAI service.
 * @param model
 */
export default ({ apiKey, model }) => {
	//
}
